---
# Prefill Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm-sim-p
spec:
  replicas: 1
  selector:
    matchLabels:
      llm-d.ai/role: prefill
  template:
    metadata:
      labels:
        llm-d.ai/role: prefill
    spec:
      initContainers:
      # vLLM render sidecar - handles tokenization via HTTP (vllm launch render)
      # restartPolicy: Always makes this a native sidecar (K8s 1.29+):
      # vllm-prefill will not start until this container's readinessProbe passes
      - name: vllm-render
        image: docker.io/vllm/vllm-openai-cpu:v0.19.0
        imagePullPolicy: IfNotPresent
        restartPolicy: Always
        command: ["vllm"]
        args: ["launch", "render", "/models/Qwen3-0.6B", "--port=8082"]
        ports:
        - name: http
          containerPort: 8082
        livenessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - mountPath: /models
          name: volume-models
          readOnly: true
      containers:
        - name: vllm-prefill
          image: ghcr.io/llm-d/llm-d-inference-sim:latest
          imagePullPolicy: IfNotPresent
          args:
            - "--v=4"
            - "--port=8000"
            - "--model=/models/Qwen3-0.6B"
            - "--data-parallel-size=1"
            - "--prefill-time-per-token=200ms"
            - "--prefill-time-std-dev=3ms"
            - "--render-url=http://localhost:8082"
          ports:
            - containerPort: 8000
      volumes:
      - name: volume-models
        hostPath:
          path: /mnt/d/models
          type: Directory
---
# Decode Deployment (with routing-sidecar + vLLM simulator)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm-sim-d
spec:
  replicas: 1
  selector:
    matchLabels:
      llm-d.ai/role: decode
  template:
    metadata:
      labels:
        llm-d.ai/role: decode
    spec:
      initContainers:
      # vLLM render sidecar - handles tokenization via HTTP (vllm launch render)
      # restartPolicy: Always makes this a native sidecar (K8s 1.29+):
      # vllm-decode will not start until this container's readinessProbe passes
      - name: vllm-render
        image: docker.io/vllm/vllm-openai-cpu:v0.19.0
        imagePullPolicy: IfNotPresent
        restartPolicy: Always
        command: ["vllm"]
        args: ["launch", "render", "/models/Qwen3-0.6B", "--port=8082"]
        ports:
        - name: http
          containerPort: 8082
        livenessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - mountPath: /models
          name: volume-models
          readOnly: true
      containers:
      - name: routing-sidecar
        image: ghcr.io/llm-d/llm-d-routing-sidecar:v0.8.0
        imagePullPolicy: IfNotPresent
        args:
          - "--port=8000"
          - "--vllm-port=8200"
          - "--connector=nixlv2"
          - "--secure-proxy=false"
        ports:
          - containerPort: 8000
      - name: vllm-decode
        image: ghcr.io/llm-d/llm-d-inference-sim:latest
        imagePullPolicy: IfNotPresent
        args:
          - "--v=4"
          - "--port=8200"
          - "--model=/models/Qwen3-0.6B"
          - "--data-parallel-size=1"
          - "--render-url=http://localhost:8082"
        ports:
          - containerPort: 8200
      volumes:
      - name: volume-models
        hostPath:
          path: /mnt/d/models
          type: Directory
---
apiVersion: v1
kind: Service
metadata:
  name: vllm-sim-p-service
spec:
  selector:
    llm-d.ai/role: prefill
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: vllm-sim-d-service
spec:
  selector:
    llm-d.ai/role: decode
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
