[build-system]
requires = ["maturin>=1.8,<2.0"]
build-backend = "maturin"

[project]
name = "router"
version = "0.1.0"
requires-python = ">=3.8"

[project.scripts]
router = "router.cli:main"

[tool.maturin]
python-source = "src"
module-name = "router._core"
